const MOVING = ['D', 'U'];

const MOVE_RESULT = {
  MOVEABLE: 'moveable',
  END: 'end',
  FAIL: 'fail',
};

const GAME_COMMAND = {
  RETRY: 'R',
  QUIT: 'Q',
};

module.exports = {
  MOVING,
  MOVE_RESULT,
  GAME_COMMAND,
};
